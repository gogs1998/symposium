# Runtime + boot-seed deps for the cloud backend image. Pinned to the versions
# verified locally. Embeddings are LOCAL (fastembed/onnxruntime, no API); the LLM
# client is the OpenAI SDK pointed at OpenRouter. YouTube libs are intentionally
# absent — ingestion at serve time only reads git-tracked jsonl/files.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
chromadb==1.5.9
fastembed==0.8.0
openai==2.46.0
pypdf==6.14.2
