**Badge** — small uppercase status marker for figure availability and counts. `live` = Published, `pending` = Coming soon.

```jsx
<Badge tone="live" dot>Published</Badge>
<Badge tone="pending" dot>Coming soon</Badge>
```

Tones: `neutral | accent | live | pending | danger`. `dot` prepends a status dot.
