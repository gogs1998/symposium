Italic opener suggestion with a bronze em-dash; clicking it should send that question.

```jsx
<SuggestedQuestion text="What would you see riding alongside a beam of light?" onClick={() => send(q)} />
```
