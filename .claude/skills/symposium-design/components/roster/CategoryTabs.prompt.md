Underlined Marcellus tab row for switching roster categories; the active tab gets a lapis underline.

```jsx
<CategoryTabs tabs={["Historical", "Creators"]} active={tab} onChange={setTab} counts={{ Historical: 25, Creators: 2 }} />
```
