The register chip — the system's only pill — lives in the chat header; opening it reveals the voice-nudge menu. Only render for personas with multiple registers (creators).

```jsx
<RegisterIndicator registers={["on-camera", "conversational", "written"]} active="conversational" onNudge={setRegister} />
```
